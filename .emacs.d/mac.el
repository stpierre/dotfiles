;;; mac.el --- macOS GUI frames -*- lexical-binding: t -*-

;;; Commentary:
;;; These only affect GUI frames.  TTY frames in kitty get the same
;;; mapping from kkp (see tui.el) and kitty.conf.

;;; Code:

(setq ns-command-modifier 'meta
      ns-option-modifier 'super)

;;; mac.el ends here
